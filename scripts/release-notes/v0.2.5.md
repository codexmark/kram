Kram v0.2.5 hardens first-run setup and expands native distribution.

- onboarding completes only after the final Ready step;
- tool presets are idempotent and apply to the live daemon immediately;
- provider setup requires a real operational check, with an explicit outage override;
- native PowerShell installer for Windows amd64;
- Android/Termux arm64 release artifact, platform detection, shell discovery, and URL opener support;
- shared context policy for fixed prompt cost, history, response reserve, and tool-output growth;
- reproducible `scripts/verify.sh` local gate and broader entrypoint/TUI coverage.

Issue status in this candidate:

- #1-#4 remain resolved and their routing/streaming regression tests pass;
- #5-#8 are fixed by this change set;
- #9 is implemented and cross-built, but still needs a clean Windows amd64 acceptance run;
- #10 is implemented and cross-built, but still needs the physical Termux acceptance matrix required by the issue.

Real configured-provider eval matrix:

- PASS `never_returns_empty_final_answer`;
- PASS `grep_never_returns_binary_garbage` (the model called grep);
- PASS `memory_saved_when_explicitly_asked`;
- PASS `memory_saved_proactively`;
- PASS `core_tools_are_registered`;
- SOFT FAIL `skill_used_when_explicitly_relevant` (the model did not call `skill_list` or a skill).

The full local gate passed: formatting, `go vet`, all packages with `-race`, host build, Windows cross-build, Android cross-build, and installer behavior/static checks. `pwsh` was unavailable on the validation host, so the native PowerShell parser and Windows runtime acceptance remain explicit validation gaps tracked by #9 and #10.
